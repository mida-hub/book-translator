# 技術仕様書：Kindle 英語読書補助用 Mac スクリーンショット収集アプリ (v1.0)

本書は、Mac のローカル環境で動作する「画面キャプチャ ＋ PNG 保存」を採用した Kindle 読書補助アプリケーションの要件定義および技術仕様をまとめたものです。

---

## 1. システム概要
本アプリケーションは、Mac 版 Kindle アプリで開いている洋書のページ（またはユーザーが指定した画面領域）を、キーボードショートカット等のトリガーによってキャプチャし、書籍タイトル名別のディレクトリにタイムスタンプ付き PNG 画像として保存するデスクトップツールです。

Kindle 本体のデータ（DRM）に直接関与しないため、安全かつ安定して動作させることができます。

---

## 2. アーキテクチャ図

```
[ Kindle for Mac ]
       │
       │ (1) 画面キャプチャ (Macスクリーンショット)
       ▼
[ 本アプリケーション (Mac Local) ]
       │
       │ (2) PNG ファイルとして保存
       ▼
[ ~/book-translator/screenshots/<書籍タイトル>/<timestamp>.png ]
       │
       │ (3) 保存完了を UI に通知
       ▼
[ アプリケーション UI ] (常に最前面表示 / 透過率・テーマ調整可能)
```

---

## 3. 機能要件 (Functional Requirements)

### 3.1. トリガー・キャプチャ機能
* **グローバルショートカット起動:** Kindle 画面がアクティブな状態から、`Cmd + Option + T` を押すことでキャプチャ処理を開始する。
* **書籍タイトル設定:** アプリ起動後、UI 上部の入力フィールドに書籍タイトルを入力する。未入力のままキャプチャを実行しようとするとウィンドウが入力を促す。
* **キャプチャ領域指定:** UI のドロップダウンで以下の 3 つから選択する。選択は次回起動時も保持される。
    * **画面左半分** — プライマリモニターの左 50%
    * **画面右半分** — プライマリモニターの右 50%
    * **画面全体** — プライマリモニター全体

### 3.2. 画像保存機能
* **保存先:** `~/book-translator/screenshots/<書籍タイトル>/` 以下に PNG として保存する。
* **ファイル名:** `YYYY-MM-DD_HH-MM-SS.png`（ローカル時刻のタイムスタンプ）。
* **ディレクトリ名の正規化:** 書籍タイトルに包まれる OS 禁止文字 (`/ \ : * ? " < > |`) は `_` に変換する。
* **フォルダを開く:** UI の 📂 ボタンで保存ディレクトリを Finder で開ける。

### 3.3. ユーザーインターフェース (UI)
* **最前面表示 (Always on Top):** Kindle 画面の上、あるいは横に常に表示され、Kindle を操作しても背後に隠れないようにする。
* **スタイリング調整:** 読書の邪魔にならないよう、背景の透過度 (Opacity)、カラーテーマ（ライト／ダークモード）を設定ダイアログで変更できる。設定はアプリ終了後も保持される（`~/.book-translator/settings.json`）。
* **マウスイベント透過 (オプション):** UI ウィンドウの背後にある Kindle のページめくりを妨げないよう、クリックイベントを透過させる設定を可能にする。`Cmd + Option + C` でいつでもトグル切り替えできる。
* **ウィンドウ位置・サイズの保持:** ウィンドウの位置とサイズはアプリ終了時に自動保存され、次回起動時に復元される。
* **ステータス表示:** 最後に保存したファイル名とセッション内の保存枚数をユーザーに通知する。

---

## 4. 技術スタック (Technical Stack)

| レイヤー | 推奨技術スタック | 選定理由 |
| :--- | :--- | :--- |
| **言語・ランタイム** | **Python 3.11+** / **uv** | 豊富なライブラリと macOS API との親和性。uv によって仮想環境・パッケージ管理を一元化。 |
| **UI フレームワーク** | **PyQt6** | 最前面表示、背景透過、クリック透過ウィンドウの制御が容易なため。 |
| **OCR エンジン** | **macOS Vision Framework** (`pyobjc-framework-Vision`) | Apple Silicon (M1/M2/M3) 上で完全ローカル・無料で高速に動作。 |
| **OS 制御/フック** | **pynput** / **mss** | グローバルショートカットの監視、画面キャプチャの実行。 |
| **設定管理** | JSON ファイル | UI 設定を `~/.book-translator/settings.json` に永続化。 |
| **配布形式** | **Python スクリプチ** (`uv run python main.py`) | `pyproject.toml` で依存関係を管理。`uv sync` 後に実行する。 |

---

## 5. データフローとシーケンス

1.  **待機:** アプリを起動すると、裏でグローバルショートカットのリスナーが待機。
2.  **トリガー:** ユーザーが Kindle で新しいページをめくり、ショートカットキーを押下。
3.  **キャプチャ:** 選択中のキャプチャ領域（左半分 / 右半分 / 全体）のスクリーンショットをインメモリ（PIL.Image）に保持。
4.  **ファイル保存:** `~/book-translator/screenshots/<書籍タイトル>/<YYYY-MM-DD_HH-MM-SS>.png` に保存。
5.  **UI 更新:** 最前面ウィンドウのステータスと保存枚数を更新。

---

## 6. 今後の拡張性 (非機能・フェーズ2要件)

以下は v1.0 のスコープ外とし、将来の拡張候補として記録する。

* **OCR + 翻訳機能の追加:** macOS Vision Framework による OCR と DeepL API を使った翻訳処理を実装し、保存画像またはキャプチャ直後に日本語翻訳を表示する。
* **Kindleウィンドウ自動検出:** Kindle アプリのウィンドウ座標を OS から自動取得し、そのウィンドウ内のみをキャプチャする。
* **変更検知 (自動翻訳):** ページがめくられたことを画面のピクセル差分で自動検知し、ショートカットキーを押さずとも自動で翻訳が更新される機能。
* **履歴・単語帳機能:** 翻訳した文章や、特定の英単語をローカルの SQLite 等に保存し、後から見返せるようにする機能。
* **ローカルLLM対応:** DeepL の代わりに `Ollama` 等を用いて、PC 内で完全ローカルかつ無料で翻訳を完結させるモードの追加。
