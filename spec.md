# 技術仕様書：Kindle 英語読書補助用 Mac スクリーンショット収集・OCRアプリ (v1.1)

本書は、Mac のローカル環境で動作する「画面キャプチャ ＋ 自動 OCR ＋ 透過表示」を採用した Kindle 読書補助アプリケーションの要件定義および技術仕様をまとめたものです。

---

## 1. システム概要
本アプリケーションは、Mac 版 Kindle アプリで開いている洋書のページをキャプチャし、OCR（文字認識）によってテキストを抽出、透過ウィンドウ上に表示するデスクトップツールです。

キャプチャ画像は書籍タイトル名別のディレクトリに保存され、抽出されたテキストはクリップボードへのコピーやファイル保存が可能です。フレームレスの透過ウィンドウを採用しており、読書を妨げずに結果を確認できます。

---

## 2. アーキテクチャ図

```
[ Kindle for Mac ]
       │
       │ (1) 画面キャプチャ (Macスクリーンショット)
       ▼
[ 本アプリケーション (Mac Local) ]
       │
       │ (2) PNG 保存 ＆ 自動 OCR (macOS Vision)
       ▼
[ ~/book-translator/screenshots/<書籍タイトル>/<timestamp>.png ]
       │
       │ (3) テキスト抽出 ＆ ファイル保存 (.txt)
       ▼
[ アプリケーション UI (フレームレス) ]
       ├─ [上部コンテナ: 不透明] (操作ボタン・入力)
       └─ [下部コンテナ: 透過]   (OCR結果表示エリア)
```

---

## 3. 機能要件 (Functional Requirements)

### 3.1. キャプチャ ＆ 自動 OCR 機能
* **グローバルショートカット起動:** `Cmd + Option + T` でキャプチャを開始。
* **自動 OCR 連携:** キャプチャ完了後、直ちに macOS Vision Framework を用いて文字認識を実行し、結果を UI に表示する。
* **書籍タイトル設定:** UI 上部でタイトルを入力。保存ディレクトリ名に使用される。
* **キャプチャ領域指定:** 左半分 / 右半分 / 全体 から選択可能。
* **マージンフィルタリング:** OCR 実行時、システム要素（メニューバーや進捗バー）の混入を防ぐため、画像の上下 5% を自動的に除外して処理する。

### 3.2. 保存 ＆ テキスト管理機能
* **保存先:** `~/book-translator/screenshots/<書籍タイトル>/`
* **ファイル構成:** PNG（画像）と TXT（認識テキスト）をペアで保存。
* **クリップボード連携:** UI 上の「📋 コピー」ボタンで抽出テキストを即座に取得可能。
* **テキスト消去:** 「🧹 クリア」ボタンで表示エリアをリセット可能。

### 3.3. ユーザーインターフェース (UI)
* **フレームレス ＆ 部分透過:** 
    * macOS の透過制限を回避するため、システム枠のないフレームレスウィンドウを採用。
    * **上部エリア:** 操作系。不透明で、ドラッグによるウィンドウ移動が可能。
    * **下部エリア:** 結果表示。設定された透過率（RGBA）が適用され、背後の Kindle が透けて見える。
* **リサイズ:** 右下のサイズグリップで自由に変更可能。
* **最前面表示 (Always on Top):** Kindle 操作中も常に前面に配置。
* **クリックイベント透過:** `Cmd + Option + C` でウィンドウ自体のマウスクリックを透過させ、Kindle のページめくりを妨げないモードに切り替え可能。
* **設定の永続化:** 透過度、テーマ、ウィンドウ位置・サイズ、書籍タイトル、キャプチャモードを次回起動時に復元。

---

## 4. 技術スタック (Technical Stack)

| レイヤー | 技術スタック | 選定理由 |
| :--- | :--- | :--- |
| **言語・ランタイム** | **Python 3.11+** / **uv** | 仮想環境管理の高速化と依存関係の厳密な管理。 |
| **UI フレームワーク** | **PyQt6** | フレームレス、WA_TranslucentBackground、マウスイベント透過の高度な制御。 |
| **OCR エンジン** | **macOS Vision Framework** | Apple Silicon に最適化された完全ローカル・高速な OCR 処理。 |
| **OS 制御/フック** | **pynput** / **mss** | グローバルショートカット監視とマルチモニター対応のキャプチャ。 |
| **設定管理** | JSON | `~/.book-translator/settings.json` による永続化。 |

---

## 5. データフロー

1. **トリガー:** ユーザーがショートカットキーを押下。
2. **キャプチャ:** ウィンドウを一時的に隠し、指定領域を撮影。PNG として保存。
3. **自動 OCR:** 保存された PNG を読み込み、上下マージンを除外して文字認識を実行。
4. **結果保存:** 認識テキストを TXT ファイルとして保存。
5. **UI 表示:** 透過エリアにテキストを表示し、詳細な進捗状況をコンソールに出力。

---

## 6. 今後の拡張性 (非機能・フェーズ2要件)

* **翻訳 API 連携:** 抽出された英語テキストを DeepL や Google 翻訳等で自動翻訳し、日本語を表示する。
* **Kindle ウィンドウ自動検出:** アクティブな Kindle ウィンドウの座標を自動取得してキャプチャ範囲を限定する。
* **ローカル LLM 対応:** `Ollama` 等と連携し、文脈に応じた高度な翻訳や要約を行う。
* **履歴ブラウザ:** 過去にキャプチャ・OCR した内容を一覧表示・検索できる機能。
